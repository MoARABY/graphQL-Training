type Query {
  products: [Products]
  productById(id: ID!): Products
  productsByPrice(min: Float, max: Float): [Products]
}

type Products {
  id: ID
  description: String
  price: Float
  reviews: [Reviews]
}
type Reviews {
  id: ID
  rating: Int
  comment: String
}
